{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": ["...", "pnpm --filter @ride-share/mobile build", "pnpm --filter @ride-share/api build"]
    }
  },
  "deploy": {
    "startCommand": "node scripts/railway-start.cjs"
  }
}
