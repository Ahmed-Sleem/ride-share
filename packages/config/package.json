{
  "name": "@ride-share/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared toolchain configuration — one source of truth for TypeScript, ESLint and Prettier (§0.3 applied to configuration)."
}
