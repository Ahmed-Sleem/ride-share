# API

The backend service. NestJS modular monolith implementing
`docs/specification/MASTER_SPECIFICATION.md`.

**Status: M0 skeleton.** The first real module (config + health) lands in
BUILD_PLAN P0.6. Until then this package is intentionally empty — nothing here
pretends to work.
