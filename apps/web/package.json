{
  "name": "@ride-share/web",
  "version": "0.1.0",
  "private": true,
  "description": "The web interface — one self-contained HTML file assembled from src/.",
  "scripts": {
    "build": "node build.js",
    "start": "node server.js",
    "test": "node tests/unit.test.js && node --test tests/server.test.js",
    "test:layout": "node tests/layout.test.js",
    "test:breaks": "./tests/breaks.sh",
    "test:layout-breaks": "./tests/layout-breaks.sh",
    "verify": "./verify.sh"
  },
  "devDependencies": {
    "jsdom": "catalog:",
    "puppeteer": "catalog:"
  }
}
