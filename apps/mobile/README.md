# Mobile

Android APK built with **Capacitor** wrapping `@ride-share/web` (DEC-176).

Created empty by design: the mobile app is BUILD_PLAN Phase 7, and the
offline-first driver flow is its core, not an addition. Nothing here pretends
to work yet.
