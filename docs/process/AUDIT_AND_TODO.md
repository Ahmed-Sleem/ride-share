# AUDIT_AND_TODO

## Open items (append-only)

| ID | Type | Description | Status |
|----|------|-------------|--------|
| G-001 | Scope risk | Addressed by DEC-110 (one corridor complete, then widen). Residual scope-vs-capacity risk is owned by the user per DEC-111/112. | CLOSED |
| G-002 | Legal | Researched (R1). HANDED OFF to legal team per DEC-030. | HANDED OFF |
| G-003 | Data | RESEARCHED (R16): a PLOS ONE study names Egypt among the worst OSM coverage (<1/3 of streets, 2017), BUT dense cities and main roads are the best-mapped, and our design (verified stops, operator routes on main corridors, no door-to-door) is largely insulated. Converted to a PHASE-2 ENGINEERING TASK: measure the Alexandria extract before committing to self-hosted OSRM. | TASK — Phase 2 |
| G-004 | Payments | CLOSED — R10 researched the rails; CH6 specifies the ledger, all payment methods behind one interface, and the exact cash sequence (DEC-078). | CLOSED |
| G-005 | Algorithm | CLOSED — CH5 v2 specifies the full 6-stage pipeline for on-demand, scheduled, recurring and route-ticket operation. | CLOSED |
| G-006 | Legal/strategy | HANDED OFF — licensing strategy belongs to the user's legal team (DEC-030). Recorded once in CH17 R-2; the technical system can satisfy per-driver permits, vehicle age limits and trip-data export whatever they decide. | HANDED OFF — legal team |
| G-007 | Data | Merged into G-003 (same task): download the Alexandria Geofabrik extract, verify launch corridors, turn restrictions, one-ways and walking routes; benchmark OSRM times against a commercial provider; contribute upstream fixes. | TASK — Phase 2 |
| G-008 | Legal/technical | SPECIFIED — CH13 §13.4 defines the technical export capability: scoped export, no standing external access, every export logged with requester/approver/scope. | SPECIFIED |
| G-009 | Scope | RECLASSIFIED: hiring and timeline are out of scope (DEC-111, DEC-112). Recorded once as a standing user-owned risk in CH17. Agent will not re-raise. | HANDED OFF — user |
| G-010 | OSS vetting | CLOSED by DEC-143 — components vetted at adoption, recorded in the implementation log. | CLOSED |
| G-011 | Architecture | CLOSED by DEC-085 — rider+driver remain ONE Expo app (DEC-014 preserved) with role-adaptive UI; driver permissions requested only on driver activation; role code lazy-loaded. Native app provides the background GPS the browser cannot. | CLOSED |
| G-012 | Delivery | CLOSED by DEC-085 — web serves riders, public site and dashboards; driving requires the native app. Web still supports driver signup, documents, schedule and earnings. DEC-015 (web first) survives as a SEQUENCING preference, now reconciled with platform reality. | CLOSED |
| G-013 | Product risk | Transfers: CLOSED by DEC-025 — no transfer entity; each leg is an independent Booking, refunded independently. Residual stranded-rider risk moved to G-015. | CLOSED |
| G-014 | Go-to-market risk | Booking-for-others: CLOSED by DEC-024 — strict self-booking only. Accepted loss: school transport, parent-for-student, HR-for-visitor. | CLOSED (accepted) |
| G-015 | Trust/safety | CLOSED by DEC-135 — multi-leg suggested normally; each leg independent; failed leg refunded like any other failure. User's rationale: same risk profile as a single leg. | CLOSED |
| G-016 | Security | CLOSED by DEC-032 — Support Agent role added with limited, explicitly-bounded powers. | CLOSED |
| G-017 | Legal (CRITICAL) | Product is an OPEN PUBLIC ride service (DEC-029), which per research R1.x sits inside Egypt's ride-hailing law. HANDED TO THE LEGAL TEAM per DEC-030. Not designed by the agent. Remains listed so it is never forgotten; the technical system must be able to satisfy whatever the legal team requires (per-driver permits, vehicle age limits, trip-data retention/export). | HANDED OFF — legal team |
| G-018 | Product gap | CLOSED by DEC-033 — three combined paths: self-registering fleet drivers, admin-entered vehicles, and an optional admin-only fleet label for grouping. | CLOSED |
| G-019 | Contradiction | CLOSED by DEC-039 — driver is the accountable entity, vehicle is a declared+approved attribute, no ownership enforcement. DEC-036 superseded, INV-12 revoked. | CLOSED |
| G-020 | Launch workload | CLOSED by DEC-040 — dedicated mapping team resourced by the user; internal Stop Mapping Tool becomes a required product component (see G-022). | CLOSED |
| G-022 | Required tooling | SPECIFIED — CH4 §4.4 (capabilities) and screens O-18/O-19 (desk + offline field mode). Now a BUILD TASK in Phase 2, not a spec gap. | SPECIFIED |
| G-021 | Product capability | CLOSED by DEC-043 — mandatory name+location, optional night-safe/accessible flags, photos and description. | CLOSED |
| G-023 | New scope | SPECIFIED — all five Manager surfaces exist: live occupancy (G-10), coverage board (G-11), pricing control (G-13), promotions (G-15), campaigns (G-17), plus alerting throughout. | SPECIFIED |
| G-024 | Design gap | CLOSED by DEC-134 — no walking rule; boarding points listed sorted by honest walking time, one recommended. | CLOSED |
| G-025 | Product quality | CLOSED by DEC-141 — kept as decided, instrumented (complaints + ratings by vehicle size), reviewed after the closed beta. | CLOSED (measuring) |
| G-026 | Design gap | CLOSED by DEC-136 — the always-visible numeric code is the fallback; manifest confirm remains as an audited override. | CLOSED |
| G-027 | Privacy/cost | CLOSED by DEC-137 — collect everything including in-app behaviour; indefinite retention with cold-storage tiering; security mitigations in CH13 §13.2 / CH12 §12.7. | CLOSED |
| G-028 | Business risk | CLOSED by DEC-130 — guarantee failure pays full day credit + compensation credit, amounts Manager-configurable. Unblocks screen R-51. | CLOSED |
| G-029 | Efficiency risk | CLOSED by DEC-084 — treated as a human/driver judgement, not system logic; app shows consequences to inform the driver. Original note: DEC-052 sets a 10-minute wait. Every minute waited is inflicted on all riders on board and all downstream stops. Mitigations to consider: only allow long waits when the vehicle is empty/near-empty, or when no downstream promise is breached; show riders on board what is happening. | CLOSED |
| G-030 | Strategy | CLOSED by DEC-057 — map city-wide, operate university routes first, expand via Manager dashboard. | CLOSED |
| G-031 | Business risk | CLOSED by DEC-141 — kept as decided, instrumented from day one, reviewed after the closed beta. | CLOSED (measuring) |
| G-032 | Required capability | SPECIFIED — CH4 §4.5 (manager-controlled activation), CH8 §8.5 (runtime configuration, no deploy), screen O-21/O-23. | SPECIFIED |
| G-033 | Design detail | CLOSED by DEC-133 — per-km is a manager pricing-suggestion tool only; riders always pay the route flat fare. | CLOSED |
| G-034 | Growth mechanic | CLOSED by DEC-139 — shareable link with sharer code; reward on the joiner's COMPLETED ride. | CLOSED |
| G-035 | Design detail | CLOSED by DEC-140 — unavailable boarding points greyed with a plain reason; boarding fixed to stops, alighting free anywhere on the route. | CLOSED |
| G-036 | Supply risk | MONITOR — fairness weight exists as configuration defaulting to 0; track driver earnings distribution and churn by decile after launch. | MONITOR |
| G-037 | CONFLICT | CLOSED by DEC-083 — multi-seat booking allowed under one rider's name. Original: F-38: DEC-024/INV-11 (book only for yourself) blocks a rider booking multiple seats for friends travelling together — a normal everyday case that Swvl supports. Needs a decision. | CLOSED |
| G-038 | Accessibility | CLOSED by DEC-136 — the numeric code is always present and screen-reader accessible, giving blind/low-vision riders a non-visual boarding path. | CLOSED |
| G-039 | Operational | CLOSED by DEC-142 — software behaviour in CH12 §12.6 plus a detailed Operations Runbook (CH18) in the master document. | CLOSED |
| G-040 | Local reality | CLOSED by DEC-118 — wall-clock scheduling (DST-safe), Ramadan mode, holiday calendar that auto-pauses subscriptions. Implementation detail lands in CH5/CH6 rewrites. | CLOSED |
| G-041 | Legal (CRITICAL) | DEC-094 keeps all personal movement data indefinitely. This must be explicitly cleared by the user's legal team against Egypt's data-protection law (PDPL) and any sector rules. The agent has flagged it once, as required; it is not designed around. | HANDED OFF — legal team |
| G-042 | Cost | SPECIFIED — CH13 §13.2 (tiered storage, not tiered deletion) and CH15 §15.3 (cold storage as a cost line). Real quotes still needed at build time. | SPECIFIED |
| G-043 | Security | SPECIFIED — CH12 §12.7 (separate encrypted movement store, pseudonymised analytics, Super-Admin-only bulk export) and CH13 §13.2. | SPECIFIED |
| G-044 | Safety (important) | CLOSED by DEC-138 — 10% report rate over a 100-ride rolling window with a 20-ride minimum triggers human review; severe categories suspend immediately; category clustering escalates; no automatic deactivation, appeal required. | CLOSED |
| G-045 | Operational risk | DEC-104 self-managed VPS: mitigations defined in CH15 §15.2 (infra-as-code, tested restores, monitoring, staging). Staffing/costing is user-owned per DEC-111/112. | CLOSED (mitigations specified) |
| G-046 | Data loss risk | CLOSED by DEC-107 — self-hosted with live replica + automated off-site backups + documented restore drills. | CLOSED |
| G-048 | Product gap | CLOSED by DEC-114 — the question dissolved: no destination is declared, so there is nothing to change. The rider simply stays on longer or gets off earlier, signalling per DEC-117. | CLOSED |
| G-050 | Documentation | CLOSED — master regenerated as v1.1, then v1.2 after the route-ticket rewrites. | CLOSED |
| G-047 | Product gap | CLOSED by DEC-113 — no automatic surge; manager-initiated temporary price changes only. | CLOSED |
| G-049 | Product gap | CLOSED by DEC-118 — wall-clock scheduling, Ramadan mode, holiday calendar. | CLOSED |
| G-051 | MAJOR MODEL CHANGE | CLOSED — DEC-115/116/117 decided the model; CH5, CH9 and CH10 all rewritten to v2 for the route-ticket model. | CLOSED |
| G-052 | Algorithm | CLOSED by DEC-119 — F6 replaced with SCHEDULE ADHERENCE (`MaxScheduleSlip` against the published route timetable). CH5 v2 written. | CLOSED |
| G-053 | Launch metric | MONITOR — instrument alighting-signal usage rate from day one (analytics G-18); if low, make the control more prominent or revisit DEC-116. | MONITOR |
| G-054 | CONTRADICTION (major) | CLOSED by DEC-132 — Model M3 adopted. Swept and fixed across 8 areas: CH02 permissions, CH01 model table + SupplyPolicy, CH03 journey states (CLAIMED), CH05 slot awareness, CH09 route_slots/slot_claims/endpoints/INV-32-33, CH10c driver claim screens (D-11/D-12), CH10d O-23 slot management + G-11 coverage board. | CLOSED |
| G-055 | Delivery risk | DEC-147 (push only, no SMS fallback) means a rider on a poor connection may never receive "your vehicle has arrived". Mandatory mitigations: local scheduled-time alarm that fires without a live push; cached waiting screen; instrumented push-delivery failure rate reviewed weekly. If failure rates are material after the beta, revisit DEC-147 for boarding-critical messages only. | MONITOR |
| G-056 | Safety risk | DEC-157 declines periodic driver face-match, accepting the risk of ACCOUNT SHARING (an unverified person driving on an approved account). Compensating controls: strict onboarding (DEC-035), incident flow (DEC-138), GPS/behaviour anomaly detection. Monitor for signals of shared accounts (device changes, driving-pattern shifts, rider reports of "a different driver"). Revisit if any signal appears. | MONITOR |
| G-057 | Deferred | OSM survey (G-003/G-007) is DEFERRED by DEC-174 — a commercial map provider removes it from the launch path. It returns only if/when self-hosted OSRM is adopted for matrix volume. | DEFERRED |
| G-058 | Deferred | BUILD_PLAN Phases 2-8 are unwritten. P0 and P1 are complete and remain valid. Resume after GUI approval (DEC-175). | **CLOSED** — Phases 2-8 written (P2 geography, P3 routes/slots/journey, P4 safety, P5 commercial, P6 recurring, P7 Capacitor APK, P8 validation/launch). |
| G-059 | Open question | Driver recommendation display (single number vs number-with-source) was not resolved before the priority change. | **CLOSED** — resolved as *a number with its evidence*: "12 · 12 riders searched this slot yesterday". Satisfies the request for a single figure without a confident forecast built on no data (§8 decorative placeholder prohibition). Enforced by test "the recommendation states its evidence" with a break case. |
| G-060 | Open | Product name (DEC-128) is still the placeholder **Sekka**; no trademark check done. Blocks the Play Store listing and the GitHub repository name, not development. | OPEN |
| G-061 | Open | **CLOSED by DEC-197** — numeric lat/lng columns + one shared distance module; no PostGIS, no host change. PostGIS remains an option behind the repository seam if a polygon service-area or true spatial volume ever demands it. | CLOSED |
| G-062 | Scale monitor | DEC-186 removed Redis. Clause 1 (distributed rate limiting) is **RESOLVED by DEC-195** — the throttler now keeps its state in PostgreSQL (`throttle_records`), so limits survive restarts and are shared across instances. Clause 2 (pub-sub fan-out latency at volume) remains MONITOR — LISTEN/NOTIFY stays correct; measure delivery latency only with measured evidence. | MONITOR (clause 2 only) |
| G-063 | Test drift | `layout.test.js` hardcoded the OLD demo sheet list (qr/topup/subs/scan/contacts/trip/fare) and fired real `file://` fetches, so 9 assertions failed. **CLOSED** — the sheet list is now derived from the live `SHEETS` registry (with a core-sheet guard), fetch is stubbed to the standard error shape, and a dedicated wide-table case keeps the scroll-wrapper guarantee reachable. | CLOSED |
| G-064 | CI gap | The web verify scripts (`apps/web/verify.sh`, `tests/breaks.sh`, `tests/layout-breaks.sh`) were committed without the exec bit (100644), so the CI `verify-gui` job's `./verify.sh` could never run. **CLOSED** — all 17 tracked `*.sh` files are now 100755. | CLOSED |
| G-065 | Break harness | Two break cases (`driver duty reverts to fake earnings`, `rider home reverts to sample routes`) targeted code lines removed in M1-finish, and the expectation matcher used regex grep on literal names containing `[object Object]` — three breaks could never be observed failing. **CLOSED** — sed edits updated to the current screens; matcher switched to `grep -F`. 74/74 breaks now caught. | CLOSED |
| G-066 | Orphaned test | `apps/web/tests/a11y.test.js` (axe-core) was not wired into `pnpm verify`, `verify.sh`, or any CI job. **CLOSED** — wired into the web `test` script and `verify.sh`. | CLOSED |
| G-067 | Doc drift | README.md, docs/README.md, PROJECT_MAP.md and 00_MASTER.md still claimed the rider journey renders demo data and M2/M3 were "not started". **CLOSED** — statuses and feature lists updated to match the code. | CLOSED |
| G-068 | Config drift | `.env.example` carried a dead `MAP_PROVIDER_KEY` (nothing reads it) and omitted the web server's `API_INTERNAL_URL` / `MAP_PROVIDER` / `GOOGLE_MAPS_API_KEY`. **CLOSED** — corrected. | CLOSED |
| G-069 | Duplicate register | `docs/process/AUDIT_AND_TODO.md` and `docs/decisions/OPEN_ITEMS.md` were two copies of the same gap register and had drifted (G-061/G-062 missing from one). **CLOSED** — one canonical register in `docs/process/AUDIT_AND_TODO.md`; `OPEN_ITEMS.md` is now a pointer. | CLOSED |
| G-070 | Live infra | **CLOSED — it was a code bug, not a Railway config issue.** The deployed API crash-looped: `node dist/main.js` exited 1 right after migrations, silently. Root cause: `JourneysModule` was missing `@Global()`, so `BookingsService` could not inject `JourneysService` and the whole Nest DI graph failed to boot — invisible because the app was created with `{ logger: false }`, so Nest's ExceptionsZone swallowed the error and called `process.exit(1)`. Fixed: `@Global()` on journeys; the real logger is now wired into `NestFactory.create`; the logger now emits an Error's message+stack; and a new DI-graph compile test (`app.graph.test.ts`) makes any future wiring break fail CI instead of production. | CLOSED |
| G-071 | Dead control | Every "live" search field rendered an `<input>` with no listener — search did nothing anywhere, and no Arabic support existed. **CLOSED** — Fuse.js v6.6.2 (Apache-2.0, vendored at build) + an Arabic/English normalization layer in `lib/search.js`; riders search routes + boarding stops with bookable journeys nested; the ops stops list filters live; dead bands on "coming soon" screens were removed (§8.1). | CLOSED |
| G-072 | UX bug | Switching the trips Upcoming/Past tabs called `render()` — the whole app rebuilt, the scroller snapped to the top (the reported "page moves"), and `/bookings/mine` was refetched on every tap. **CLOSED** — the list is fetched once into `S.tripsCache` and the tab switch re-renders it in place (no refetch, no scroll jump). | CLOSED |
| G-073 | CI infra | **GitHub Actions has not EXECUTED since 2026-08-17 ~22:57 UTC** — every run on every later commit showed all jobs `failure` within ~3 seconds, no steps executed, no logs stored (billing signature: Actions minutes/spending limit exhausted). Last real run: c20bd5e (2026-08-17 22:57, only Verify GUI failed — G-064); last fully green: e78f097. **CLOSED 2026-08-24** — DEC-203: the owner made the repo public (free standard-runner minutes). Proof: a re-run of the failed jobs on HEAD `2cf3f99` executed for real — Verify ✅ (54s), Verify database ✅ (54s, also proving the G-074 fix in real CI), Build images ✅; GUI suite ran to completion. Local verification had already been independently re-proven green the same day. | CLOSED |
| G-074 | CI gap | **The `verify-db` CI job can never pass on any commit since M1.5 (9063d2b)**: `check-db-types.sh` regenerated `db.generated.ts` from `DATABASE_URL`, which in CI is the EMPTY service database — it produced `[]`, which never matches the committed file that lists real tables → guaranteed red the moment Actions minutes return. Reproduced locally 2026-08-24 (empty DB at DATABASE_URL → `✗ FAIL: drifted`). **CLOSED same day**: `check-db-types.sh` now builds its own scratch database FROM THE MIGRATIONS and generates/compares types against that — the caller's `DATABASE_URL` no longer needs to carry the schema (identical behaviour locally and in CI). Break-observed (§0.2): a staged corruption of `db.generated.ts` makes the check fail with `✗ drifted` (exit 1); empty-`DATABASE_URL` (the old failing CI condition) now passes; full `pnpm db:verify` green against both a schema-carrying and an empty database. **Validated in real CI**: the verify-db job is green on the `2cf3f99` re-run. | CLOSED |
| G-075 | Publish hygiene | Owner intended to make the repo public to restore free CI (G-073). Inventory presented (process/planning/research/spec + investor/reference PDFs; git history carries it all). **CLOSED 2026-08-24 — DEC-203**: the owner chose "everything public, no problem" with the full inventory and the clean secret scan (68 commits, one noreply author, zero secrets in tree and history) in front of them. No sanitization, no rewrite. The repo-standard departure is owner-approved and recorded in the decisions register. | CLOSED |
| G-076 | Payments correctness | The Paymob webhook verifier (`webhook.verifier.ts`) signed `JSON.stringify(payload.obj)` — the WHOLE JSON. Paymob's official scheme (confirmed on two official doc sites during R20 research) HMAC-SHA512s the VALUES of a fixed 20-field ordered list (amount_cents … success; nested order.id / source_data.*; missing → empty string), hex lowercase. The old code would have rejected every legitimate Paymob webhook the day keys went live. **CLOSED 2026-08-24**: rewritten to the official algorithm; tests rebuilt with an independently-written literal concatenation (not a mirror of the implementation) + a regression guard that the old whole-JSON scheme is REJECTED; §0.2 break-observed (removing `order.id` from the field list fails exactly the 3 signature tests); 168 API tests green. | CLOSED |
| G-077 | CI gap | **The `verify-gui` CI job failed with "Could not find Chrome" on its first real run since 2026-08-17.** Root cause: `actions/setup-node`'s `cache: pnpm` saves the pnpm store; on cached installs pnpm marks reused packages as already built and SKIPS their postinstalls — puppeteer's postinstall is the Chrome download, and Chrome lives outside the store (`~/.cache/puppeteer`), so it existed only on the very first (cold-cache) run. Invisible earlier: the single green GUI run (e78f097) predates the cache, and CI billing was dead (G-073) until today. **CLOSED 2026-08-24**: the job now caches `~/.cache/puppeteer` (keyed on the puppeteer pin) and installs Chrome explicitly (`puppeteer browsers install chrome` — idempotent, downloads only on miss). Break-observed = the real red run on `1640f6d`; proof of fix = the next run green. | CLOSED |
| G-078 | Payments correctness | Found during P3.7: `PaymobAdapter.normalizeWebhook` mapped **Paymob's numeric order id** into `PaymentEvent.orderId` while the interface contract (and the ledger join) require **OUR merchant_order_id** — every real webhook would have failed the order lookup. **CLOSED 2026-08-24**: mapping fixed (provider numeric id now travels as `providerOrderId`), regression test added (`orderId is OUR merchant_order_id, not the provider numeric id`). | CLOSED |
| G-079 | Open point | Launch commission **value** is still the owner's. The mechanic + owner Settings page exist (DEC-208). Default remains 0 until you type a percent on Settings (or set the Railway env). Agent does not invent the number. | **OPEN — owner sets the %** |
| G-080 | Infra | Railway auto-imported `mobile` had no start/build so the component failed while api/web ran. **CLOSED 2026-08-24 (P7.1)**: `apps/mobile` builds the web wrap and serves `/healthz`; Dockerfile pins PROJECT=mobile. Owner must point the Railway service at `apps/mobile/Dockerfile` with repo-root context. |
| G-081 | Duplication + type bug | **The interface asked for its own font chain twice, and one surface not at all.** `packages/brand/brand.json` names the families (§0.3, DEC-168), but `src/styles/shell.html` `body{font:…}` carried a second literal copy of the same eight names, and `.landing{}` had no `font-family` at all — so the poster rendered in whatever the user agent called its default face, metrics differing per device, and a font change could only ever be half-applied. It also made the Arabic work unexpressible: a self-hosted face has to be *asked for*. **CLOSED round 5**: `body` and `.landing` both read `var(--brand-font)`, the literal copy is gone, and `assets/fonts/fonts.json` (files, ranges, licences, sha256) is checked against brand.json by `build.js` — a family the chain does not name is held out of the bundle, and a face whose bytes moved refuses the build. Proof, measured: the Latin advance width is identical with and without the new faces (498.07 px on the probe string), the Arabic one moves to Cairo (350.78, against 362.56 without it), and `layout` 8,185 + `a11y` 14 stayed green across the change. |
| G-082 | Duplication (CLOSED 2026-09-04, owner's call) | **Two sentences of copy are rendered nowhere, and one builder branch is unreachable.** `landingHeroSub` and `driveReqB` exist in `src/data/content.js` in both languages and no module reads them; `mkStep(n, t, null)`'s no-body path lost its only caller when the driver requirements grew bodies (R6-6). Nothing was deleted — the rule is to report first. *Fix if accepted:* delete the two keys and the branch, or put the sentences to work; the parity guard would then require both locales for the remaining keys, which is already true. | **Removed after a backup.** `archive/landing-dead-code-before-removal-2026-09-04-8ce3b0a.zip` holds `apps/web` and `packages/brand` at `8ce3b0a` — the last commit carrying them — and the next commit deletes both copy keys, `mkStep`'s optional body and the unused `Katibeh` face; the font manifest, the files on disk and the stylesheet are now required to agree, which is the guard that made the removal safe (the older form passed vacuously once the face was gone). |
| G-083 | Motion (closed in R6-7) | The journey's animation loop re-armed itself unconditionally, so the landing ran 60 writes a second while the section sat settled and off-screen. It now paints the exact target and sleeps until `readScroll()` wakes it (scroll, resize, ResizeObserver, fonts). Measured in headless Chrome while dragging the page: worst frame 57.8 ms → 33.8 ms, long tasks 0, `load` 71 ms, 0 subresources. |
| G-084 | Motion (closed in R6-7) | **A live page could stay under the curtain.** `pagefx.js` drove the wipe from `requestAnimationFrame` and gave a deadline only to the *swap*, so where frames stop arriving (backgrounded or occluded window, a window restored from minimise, a starved compositor) the page painted correctly underneath and never became visible: `.pagefx` stayed in the document forever. Both paths now share one idempotent `settle()` armed by `FALLBACK_WIPE_MS`, summed from `PHASES` so the deadline cannot drift from the animation it guards. The landing suite proves it deterministically by stubbing `requestAnimationFrame` to a no-op. Surfaced as a suite failure — `theme: the curtain is taken down and the page is intact` — which was a bug report, not flake. |
| G-085 | Owner decision (closed by reversal) | **The journey's corridor is deliberately the single-scale map.** Round 6 fitted the drawn route to the text it carries (road spans first cut to last, 84-86 % of the section width); the owner saw it and preferred the older geometry, which reads as a road rather than a spread. Reverted in R7-1 to `27328c9`'s layout with only the sleeping-loop fix kept. **Do not "fix" the route span again** — the guards that demanded it were deleted on purpose, and this row is why. |
| G-086 | Copy resolution (closed in R7-3) | **A missing copy key printed its own name instead of failing.** `t(k)` ends in `?? k`, which is right for a stray key at runtime and wrong as a build-time story: deleting `policyTemplateNote` left two live call sites rendering the literal word `policyTemplateNote`, and no test noticed until the text was read. A guard now requires every key named in the landing builders (75 of them) to exist in both locales; the fallback itself stays, because a runtime miss must not throw inside a paint. |
| G-087 | Duplication (OPEN, phase 2) | **The app draws its own second map.** `src/lib/map.js` opens by declaring `RouteMap` the ONE data-bound map primitive and forbidding a hand-rolled second one (§0.3); `MapView` in `src/lib/components.js` nevertheless still builds its own SVG fallback with its own layers. `RouteMap` is called 9 times (driver, planner, rider, staff), `MapView` 4 times (staff only). *Fix:* move the four staff call sites to `RouteMap`, archive `MapView` (the `archive/` backup rule, G-082), delete it, and let one primitive own both the real-tile path and the labelled fallback. | OPEN — in P2-A |
| G-088 | Dead code (OPEN, phase 2) | **Two screens no navigation reaches.** `comingSoonRider()` appears exactly once in `src/` — its own definition; `opsStops()` has no row in `PAGES` (`breaks.sh` records the retired guard: "the CSV control is in `opsStops()`, which no nav reaches"). Their copy keys (`comingSoon`, `bookingComingBody`) are therefore rendered nowhere. *Fix:* the G-082 workflow — backup into `archive/`, then delete, then let the copy-parity guard keep both locales honest. | OPEN — in P2-A |
| G-089 | Comment vs code (OPEN, phase 2) | `SHEETS` in `src/shell/app.js` says "Safety & support actions arrive in M4 — honest placeholders, no fake calls", but `sosSheet()` builds a real `Banner` + checkbox + `sendSos()` and `reportSheet`/`shareRideSheet` are wired the same way (2 references each: definition + the `SHEETS` row). Either the comment is stale or the sheets stop short of a real request. *Fix:* trace all three against `src/lib/api.js`, implement or correct, then delete the misleading comment; a guard should make "a wired control that does nothing" un-shippable. | OPEN — verify in P2-A |
| G-090 | Copy completeness (OPEN, phase 2) | **The app has 643 `t()` sites and no completeness guard.** 364 distinct keys, 20 built at runtime (`t("roleLabel."+r)`, `t("j_cat_"+x)`, `t("stopStatus."+s)`, `t("j_dec_"+d)`, `t("w_reason_"+r)`) — the shape the landing's literal-key guard (G-086) cannot see. Measured today: **0** raw keys and **0** `undefined`/`NaN`/`[object` strings across 172 renders (43 pages × 2 languages × 2 themes) of the built file. *Fix:* keep it at 0 permanently by rendering every `PAGES` entry both languages/both themes in `unit.test.js` and asserting no visible text node is a whole key-shaped token. | OPEN — in P2-A |
| G-091 | Design system (OPEN, phase 2) | The renewal's display face exists in exactly one selector of the 1,608-line stylesheet (`--brand-font-display` in `[dir="rtl"] .landing__featuret, .landing__cta-t`), so the marketing half speaks with two faces and the app half with one. Whether the app adopts display type, and where (titles and empty states only, never controls), is the owner's call — recorded as Q-2 in `planning/APP_GUI.md`. | OPEN — decision Q-2/Q-3 |
| G-092 | Reference material (OPEN) | Phase 2 has no demo. The only export in `~/uploads/` is the landing page (`-RIDE-SHARE-COMPL (3).html`: one `<style>`, one `<script>`, classes `hdr nav panel faq policy dl-card`; `shift` and `verify` appear 0 times). "Strictly look-alike the demo" therefore needs the owner to supply the app demo or authorise the landing system as the reference — Q-1 in `planning/APP_GUI.md`. | OPEN — decision Q-1 |
| G-093 | Component reuse (OPEN, phase 2) | **More than half of the app's headings bypass the shared `Section()`.** 17 `Section(` calls across `src/screens/*.js` against 20 hand-rolled `class:"t-head"` nodes (14 of them `$("h2",{class:"t-head"}`), so a head is styled per screen rather than per concept. It surfaced while reviewing the phase-2 skin: the rule for `.section__head .t-head` did not reach "My journeys" on the driver duty screen. *Fix:* route the heads through `Section()` (§0.1.3 / §5.1), then one rule styles every page head in both themes and both languages. | OPEN — in D-1.3 |
| G-094 | Duplication + off-system colour (CLOSED, round 9 — residual closed by G-099) | **The APK's offline screen shipped its own design system.** It declared `--bg:#EEF0F4; --ink:#15181F; --brand:#6C63FF; --brand-2:#5A4FD9` (dark: `#8571FF/#A795FF/#241F5E`), painted its primary action with a 135° gradient of those stops and its logo with an SVG gradient of the same — 12 hard-coded hexes; measured on the rendered button as `background-image: linear-gradient(135deg, rgb(108, 9…`. Fixed in `apps/mobile/offline.html`: the palette is now the app's ink (`#FFFFFF` paper / `#0A0A0A` ink / `#525252` muted, plus a dark block), the button is flat, the logo follows `currentColor` the way the web logo does, `--brand-2` is gone with 0 references, and the typeface arrives from `brand.json` through an asserted `/*__RS_BRAND__*/` injection in `scripts/build.js`. The app's CSS is deliberately **not** inlined: the boot page paints before any request exists, and ~120 KB of stylesheet is the wrong price for a splash. The colour-source question is G-099. | CLOSED |
| G-095 | Generated file + native chrome (HALF CLOSED, round 9) | **Restated in round 9, because the original fix was wrong.** `apps/mobile/capacitor.config.json` is *generated* by `scripts/build.js` from `packages/brand/brand.json`, which already carries `browserThemeColor:{light:#FFFFFF,dark:#0A0A0A}` — so the committed `#6C63FF` was never a value to edit but stale drift of a build artifact. **Drift half: closed** by running the generator (no hand edit): the committed file now reads `backgroundColor:"#FFFFFF"`, `webDir:"www"`, `androidScheme:"https"`, `hostname:"localhost"`, so a light-mode cold start flashes paper rather than violet, and the value has one source. **Native half: open, binary only** — `SplashScreen.backgroundColor` is single-valued (`brand.browserThemeColor.light`), so a *dark-mode* cold start still flashes paper-white before the boot page paints; that needs Android `values-night`, and `www/index.html` is baked. `StatusBar.style:'DARK'` is a pre-JS default only (`packages/platform/src/index.js:204-207` re-sets DARK/LIGHT per resolved theme, correctly). The rule recorded from this: read the generator before "fixing" a generated file. | OPEN — binary only |
| G-096 | Chrome vs content (CLOSED, round 9) | **The top bar was the wrong object and was costing 96 px of dead space.** `header.topbar` held `[back] [h1] [actions]` at `min-height:60px`, where the h1's UA `0.67em` top margin is swallowed by `align-items:center`. The owner asked for the page to open with its title instead. Applied in `src/styles/shell.html` + `src/shell/app.js`: a two-row head grid (controls above, `--f-poster` title below) rendered as the first block of `.main__inner`; `--topbar-h` deleted (0 refs); the safe-area floor moved onto the head; `position:sticky` + `var(--glass)` + blur so content passes under it readably. Demo numbers carried into the repo build: head 219→123 px, `headFloor` 56, edges 298/298/298 (was a 175 px mismatch), 172 renders with 0 overflow / 0 clipped / 0 raw keys. | CLOSED |
| G-097 | Centralisation (OPEN, phase 2) | **Two component libraries for one product.** The renewal built 22 `mk*` builders in `src/lib/landing-parts.js` (`mkSection mkEyebrow mkDisplay mkLede mkSteps mkSlab mkPanel mkDoc mkProse mkEnd …`) and the landing uses them; the app keeps its own 18-component kit, with 17 `Section()` calls against 20 hand-rolled `class:"t-head"` heads, and its own card/table/row/metric families. The owner's instruction is explicit — the app must be "centralized, consistent, and have the same components code design like the landing page". *Fix:* one library consumed by both surfaces (the app imports the builders, or the builders absorb the app's needs), plus a guard that no screen hand-rolls what a builder already owns. Note the app's density scale (DEC-200) already rescales the shared steps — `--s6` 24→22, `--content-max` 840→756 — so shared components inherit density for free, which is the payoff. | OPEN — D-1.10 (part done: the page head is now one shell-rendered component serving all 43 pages; the 22 `mk*` builders vs the 18-component app kit split stands) |
| G-098 | Dead field (CLOSED, round 9) | **`packages/brand/brand.json` carried `logo.gradient:['#6C63FF','#5A4FD9']`, the last violet in the brand source.** Grepping every JS in the repo found no reader (the only `gradient` hit is `motion.js`'s own radial mask) and the web logo is guarded to `fill:currentColor` (`unit.test.js:422`), so the field painted nothing — it only invited a copy of itself, which is exactly what `offline.html` had done (G-094). Key deleted; `logo.color{light:#0A0A0A,dark:#F2F2F2}` is what remains and what the generator reads. | CLOSED |
| G-099 | Centralisation (CLOSED, round 9, owner approved) | **The ink palette now has one source.** After round 9's first pass the boot page mirrored the app's colours by hand, and the mirror was already wrong: its muted was `#525252` where `apps/web` defines `--text-secondary:#5C5C5C` (dark: `#A3A3A3` against `#B8B8B8`) — a grey step out of step, in the one surface no browser test looks at, because it lives inside the APK. Now `packages/brand/brand.json` carries `palette.{light,dark}{paper,ink,muted,hairline}`, `scripts/build.js` injects the three the page reads (+ `--brand/--on`) into `www/offline.html` in both themes, `hairline` is deliberately not injected (nothing consumes it — a token nothing reads is a bug waiting for a reader), and `apps/web/tests/unit.test.js` holds the stylesheet itself to those values in both themes, so neither side can drift again. Measured on the generated file: light `rgb(255,255,255)`/`rgb(10,10,10)`, dark `rgb(10,10,10)`/`rgb(242,242,242)`, `document.fonts.size === 2`, and **0** hex literals anywhere in `apps/mobile/offline.html`. | CLOSED |
| G-100 | Token hygiene (CLOSED, round 9) | **The app CSS read a token nothing defines.** `--f-small` sized `.landing__railink` (the policy rail's link label) while the scale declares `--f-display/--f-title/--f-head/--f-lg/--f-body/--f-cap/--f-micro`; an undefined `var()` makes the declaration compute to nothing, so the label inherited the body size (16/15px instead of 13px) and no test noticed, because the element still rendered text. Found by writing the general check rather than fixing the instance: a new `unit` group collects every `--x:` in the stylesheet and JS (including `setProperty("--x")` for runtime tokens like `--view-h`) and asserts that no `var(--x)` in the app CSS lacks a source — 189 used against 212 declared, exactly one missing. The typo was mine, from round 5 (`git log -S --f-small` → `059bcef`), and shipped unseen. Fixed to `--f-cap`; the guard now fails on any future typo. The same sweep disproved a suspicion: `--hairline` is used nowhere (the app's hairline is `--line`), and `.authmain`/`.authfoot` matched no element in any view — both deleted, see APP_GUI.md §11. | CLOSED |
| G-101 | Invisible control (CLOSED, round 9) | **The one anchor in the product could not be seen or clicked.** `src/lib/components.js:170` listed `"a"` in `SVG_TAGS`, and `$()` picks the namespace from that set — so `$("a", …)` created an **SVG** anchor. In an HTML document an SVG `<a>` with no width and height is a zero-box element: it carries `class="btn btn--primary"`, the right `href` and `download`, `textContent` reads "Download Android APK", and it renders nothing. Measured before the fix: `0×0`, `namespaceURI=SVG`; after removing `"a"` from the set: `238×44`, HTML ✓ the landing's download button the owner asked for. This is why the ask existed: the button was never missing from the code, only from the screen. Nothing caught it because every assertion read attributes or `textContent`. *Fix:* the namespace now comes from the tag being drawing-only, and an SVG anchor must be created with `createElementNS` inside its `<svg>` parent (no call site needs one today — `grep '\$("a"' src/` returned exactly one). *Guard:* 7 assertions added to `landing.test.js` in the download page's own loop — namespace, a box a finger can hit (≥24px), the file name and `?v=` stamp, a label, the QR still present beside it, and no `.btn/.card/.row` in the landing built in the SVG namespace. Landing suite: **2773 passed, 0 failed**. | CLOSED |
| G-102 | CI packaging (FIXED, round 9c) | **`apps/web`'s GUI job could never have gone green: exit 126**, which is "cannot execute", not a failing assertion — and the job died in 21 s, before a single test ran. `apps/web/package.json` verifies as `./verify.sh`, which ends with `./tests/breaks.sh` and `./tests/layout-breaks.sh`, while **all 23 `*.sh` files in this repo are committed 100644**. The `pnpm` jobs that call `bash scripts/x.sh` never notice, so the app's full browser suite (including both break-detection passes) has been silently unrunnable in CI. Fixed with `git update-index --chmod=+x` across the set — `git ls-files -s` now reports 100755 × 23, and `apps/web/verify.sh` is `-rwxr-xr-x` in the tree with `bash -n` clean. Consequence: `breaks.sh` (~45 min, mutates `src/` in place) will now run in CI, which is safe in a throwaway checkout. Standing lesson: a 126/127 in a CI log is a packaging fault; read the job log before blaming the environment or the tests. | FIXED |
| G-103 | Dead control (FIXED, round 10) | **The landing page's download button and QR could not have worked, and no test could have told us.** `apps/web/server.js` answers `/download/android` from `ANDROID_APK_PATH`, `apps/web/downloads/android.apk` or `uploads/app-debug.apk` — none of which exist in a deployment — so the route returned a JSON 404 (`APK_NOT_STAGED`): measured on live, `https://ride-shareweb-production.up.railway.app/download/android` = **HTTP 404**. CI did build the APK, but the job that does it was `needs: verify`, and `verify` was red, so the job was *skipped* and even then it only uploaded a workflow artifact, which needs a GitHub login to fetch — a QR pointing at an artifact is a control that pretends. Fix: CI's `apk` job gained `permissions: contents: write` and a publish step that copies the debug build to the brand's own `download.apk` name and pushes it to a rolling `android-debug` prerelease (`gh release upload --clobber`, no third-party action, gated to pushes to main); the server now redirects `/download/android` to that asset, with `ANDROID_APK_URL` as the override and the JSON 404 kept for when nothing is configured. `server.test.js` — which had **no** download test at all, the actual reason this survived — now covers staged-file 200 + attachment headers, both URL forms, the brand-derived 302 and the override; and its response fake became a real `Writable`, because `Readable.pipe` had been throwing into a `.catch(() => {})` and hanging three tests instead of failing them. Repo is public, so the asset is fetchable by a phone with no login. | FIXED |
| G-104 | Renewal gap (FIXED, round 10) | **The first-open tutorial was never deleted — it was unreachable and inconsistent.** `introView()` is a four-slide slider with dots, gated by `rs.intro.v1` and by `isAppSurface()`, whose comment states the decision: intro slides belong to the mobile product, never the public website (phone browser included) — which is why opening the site shows no tutorial. Measured on the built page: 27.3 px mixed-case headline at weight 750 while every page head in the app is an uppercase 850 poster; the footer ran the full 1280 px while the stage was a 480 px column (different edges on the same screen); a head box that changed from 51 px to 83 px between slides, moving the headline 26 px on a phone and 11 px on desktop; and a slider that ignored a swipe (`S.introSlide` stayed 0). Fixed inside the system rather than beside it: the headline now takes `--lh-poster`/`--track-poster`/`--fw-heavy` + uppercase while keeping its own `--f-intro` size, because `--f-poster` at desktop (71.68 px) wraps these sentences into three or four lines inside a 480 px column; `--intro-col` is declared once and shared by the stage and the footer's `padding-inline:max(...)`; both text slots reserve height under `@supports (min-block-size:1lh)` (2 lines for the head, 4 for the body — the reserve measured off the longest slide, not guessed), which is what makes the head top constant at 338/332/363 px across all four slides in all three viewports; `touch-action:pan-y` plus pointer handlers turn a page on a horizontal drag ≥24 px that beats the vertical component, and are a no-op past the last slide so a slipping thumb cannot press Get started; the driver slide's fifth element (`j_intro4C`) was folded into its body copy in both languages so no slide carries a part the others lack, and `.intro__c` was deleted rather than left unreferenced. A replay row — `How the ride works` / `كيف تعمل الرحلة` — now sits in the rider and driver profiles (not staff: the four slides are rider/driver copy), rendered only on the app surface so it is never a control that does nothing, and it does **not** mark the tour seen. 13 assertions added; two existing intro guards were re-pointed at the merged key because their premise (a separate `j_intro4C`) legitimately went away. | FIXED |
| G-105 | Type scale (FIXED, round 10b) | **The splash wordmark was a headline.** `--f-word` was `clamp(1.8rem,6.5vw,5rem)`, so `Ride Share` measured 28.8 px on a phone and hit the **80 px** ceiling from 1231 px up, in a box 418×84 — nearly as tall as the 92 px logo above it, which inverted the mark/wordmark hierarchy on the one screen whose only job is to be brief. It is used by exactly one rule (`.splash__name`), so the token itself was retuned rather than adding an override: `clamp(1.5rem,1.15rem + .9vw,1.9rem)` → **24 / 25.3 / 29.9 / 30.4 px** at 390 / 768 / 1280 / 1600, which also agrees with the installer's own boot page (`.name{font-size:22px}`). A unit guard now pins the shape (rem ends, a vw middle), the ceiling at ≤ 2rem and the floor at ≥ 1.3rem, so it cannot balloon back or shrink past what the binary already calls legible. | FIXED |
| G-106 | Binary plane (FIXED in code, needs a new APK to be seen, round 10b) | **Dark-mode cold start flashed white.** The generated Android project ships one theme for both modes: `values/styles.xml` gives `AppTheme.NoActionBarLaunch` an `android:background` of `@drawable/splash`, and the Capacitor template (read from `@capacitor/cli` 8.5.0's own `android-template.tar.gz`, not guessed) contains **no `values-night`** at all. New `apps/mobile/scripts/apply-android-night-splash.js` writes `values-night/colors.xml` + `styles.xml` after `cap add android` — the night splash paints `brand.json palette.dark.paper` (#0A0A0A) and sets `windowSplashScreenBackground` so the API-31 system splash agrees with the WebView that follows — and it refuses to proceed if the day theme no longer carries the anchor it mirrors, so it cannot ship a dead resource. `config.test.js` now **executes** the script against a fixture holding the template's real `values/styles.xml` and asserts the output, idempotency on a second run, and the loud failure. Day behaviour is untouched; `capacitor.config.json`'s single-valued `SplashScreen.backgroundColor` stays as is. Devices that already installed an earlier build keep the flash until the installer is reinstalled. | FIXED (binary pending) |
| G-107 | Distribution (DONE, round 10b, owner instruction) | **The installer is kept in the repo, where `apps/web/server.js` already looks for it.** The owner's reasoning, and it is sound for this project: the GUI updates over the air, so a new binary is a rare event and the history pays for the file once per release rather than per interface change. CI's `apk` job gained a step that copies the built debug APK to `apps/web/downloads/android.apk` and commits it **only when its sha256 differs**, refuses above 60 MB, and signs `[skip ci]`; the `android-debug` release publish added earlier stays as the fallback the server 302s to when no file is staged. One thing had to be surfaced rather than silently overridden: `.gitignore` line 34 is `*.apk`, a deliberate earlier decision to keep binaries out, so a narrow `!apps/web/downloads/*.apk` exception was added with the reason in the file, and `git add -f` is used. Consequences accepted knowingly: every clone carries the file (~10-30 MB), and reclaiming that size later means rewriting history, not deleting a line. Not yet evidenced: this sandbox cannot build an APK (Java yes, but no Android SDK, no gradle, 2 CPUs / 1.9 GB RAM), so the first commit of the file happens in CI on this push — measured in D-8.1 before anything is called done. | DONE (measured on CI + live) |
| G-108 | Process (FOUND + WORKED AROUND, round 10b) | **A commit message of mine silenced CI for an entire push.** Round 10b's message described the installer-commit step and quoted the literal skip marker it signs (`signs `[skip ci]``), and GitHub honours that string in the head commit of a push — so the push carrying the api fix, the release publish and the night-splash step started **no run at all**: `/actions/runs?branch=main` still showed `a5465f2` as the newest, three hours older than the push. Nothing was broken in the code, which is what makes it expensive: a green-looking repo with no verdict, discovered only by asking for the run list. Fix: an empty commit whose message says "the skip marker" in words instead of writing it (`c6a6ba0`), which started the run. Rule kept: **never write the literal `*** skip ci` form in a commit message, log line or doc snippet that a reviewer may copy** — quote it with the asterisks or spell it out, and when a run is expected but absent, read `/actions/runs?branch=main` before doubting the workflow file. | OPEN (doc-only follow-up) |
| G-109 | Guard maintenance (FIXED, round 10c) | **Three break-cases had gone blind when round 9 moved the head.** CI's first-ever `Break-detection` run finished `breaks caught: 110  missed: 3`, and all three reported `BROKEN-BREAK … edit did not change the file`: their `sed` anchors still pointed at `if(band) body.prepend(band);` and `.topbar{flex:none;display:flex;`, both of which round 9 replaced (`inner.insertBefore(band, inner.children[1]||null)`, a sticky grid head). My own mistake, and a specific kind of it: I re-anchored the **assertions** in round 9 and left the **harness that tests the assertions** pointing at prose from the old markup. Fixed by re-anchoring the three cases to the current code while preserving each one's intent (band promoted to chrome / band buried under the list / head free to shrink), and updating their expected-failure names to the assertions that carry those facts today (`band is inside the scrolling region`, `band follows the page head in the column`, `top bar cannot shrink`). Verified with the harness's own single-case filter — `BREAKS_ONLY="pinned above"` → CAUGHT, `"drops below"` → CAUGHT, `"top bar loses"` → CAUGHT — each in about 20 s instead of 18 min, and the tree restored clean afterwards. Rule kept: **when a render-path or CSS rewrite moves a guarded structure, the break-case anchor moves in the same commit**; `BREAKS_ONLY` exists for exactly that review loop, so there is no excuse for deferring it. | FIXED |
| G-110 | The app could never leave its offline card (FIXED, round 11) | **The installed APK told every user to check their internet, on a connected phone.** Read from the artifact rather than inferred: `assets/capacitor.config.json` inside the shipped APK has no `server.url` (correct - `build.js:57` says the app must start from a file, and `config.test.js:43` guards that), and `assets/public/index.html` is byte-identical to `offline.html` (sha256 `688346062f21c962`, 99,194 B) because the boot page *is* the entry file by design. Two things then broke it. (1) `liveOrigin()` resolved the OTA address from `MOBILE_PUBLIC_ORIGIN`/`RAILWAY_PUBLIC_DOMAIN` - variables that only exist on the Railway runtime - so a CI-built installer baked `origin = ""`, and `boot()`'s `if (!origin) return showOffline()` jumped to the card with no attempt, no status, no splash: Retry re-ran the same no-op. (2) Even with an origin, `apps/mobile/server.js` sent no `access-control-allow-origin` and had no `OPTIONS` handling, while the boot page always attaches `x-rs-app-id/x-rs-ts/x-rs-sign`, which are not CORS-safelisted - so the WebView preflighted into nothing and every OTA read was blocked. Fixed by making the origin rule one module (`apps/mobile/scripts/resolve-origin.js`: env in the documented order, `packages/brand/brand.json app.origin` as the floor, **a thrown build error rather than an empty string**, `new URL()` so a bare `https://` or a base path cannot pass). **The floor names the *mobile* service, verified live**: `ride-sharemobile-production.up.railway.app/healthz` → 200 `{"service":"mobile","api":"up"}`, while the web origin answers `/v1/mobile/update` with 404 - a default copied from the landing's download link would have shipped the same symptom with more confidence. And by opening the four OTA paths to the app's own local origins with a real preflight (scoped: an arbitrary origin gets nothing, and the proof check still runs on the GET). The boot page now says what it tried (`Connecting to <host>`, `<host> did not answer in time`, or - for a mis-baked build - `This build was not given a server address`), repeats the reason where it is visible (the old status line lived inside the splash, which `showOffline()` hides), and gives Retry a busy state so a second tap cannot be swallowed. Guards: 5 new in `config.test.js` (clean-env resolution, precedence, `https` enforcement, path/host rejection, and that `allowNavigation` still contains the brand host) and 2 new in `server.test.js` (preflight answered before proof, ACAO for `https://localhost` only) - mobile suite 15 → **22/0**. `brand.json` is inlined into the web bundle as `const BRAND`, so `build.js` now strips `app` before injection: the `no deployment host is written into the bundle` guard caught the leak and was satisfied by the fix, not by editing the guard. **Needs a new installer on every device** (the origin and the boot page are baked); `version.code` 3 → 4 so the binary track is unambiguous, and the download link's `?v=` follows it. | FIXED |
| G-111 | The landing's mobile menu: three defects, one of them mine twice (FIXED, round 11) | Owner report: the three-dot menu was not glassy, was not full screen, and scrolled the page to the top every time it opened. Causes, in order of finding: (a) the panel was appended **inside `header.landing__nav`, which carries `backdrop-filter`** - a filtered ancestor becomes the containing block for `position:fixed` descendants, so a full-screen sheet was structurally impossible there; it is now a sibling, built by the one `landingMenuSheet()` that the render path also uses, at `--z-landing-menu:19` under the bar's 20 so the control that opened it stays reachable. (b) "not glassy" was not the material - the panel already had `--glass` + `--glass-blur`, and the browser suite measures the alpha against the bar's (they matched at 0.62) - it was that a strip under the bar frosts the bar's own flat hero, so there is nothing to blur; over content, the same tokens read as glass, measured `blur(20px) saturate(1.8)` at 390px. (c) The scroll jump was `render()`: `.landing` **is** the scroller, `root.innerHTML=""` replaces it, and the offset died with the node - so a disclosure had to carry the reader's position, and the language and theme switches in the same bar lost it the same way. My first fix restored it inside `render()`; the browser suite then failed with `{before:1019, after:991}`, which was *not* my restore: `scroll-behavior:smooth` animates the assignment, and near the foot of a page a lazily painted map means `scrollHeight` is still growing, so the value clamps. Right answer for a disclosure: **do not re-render** - `setLandingMenu()` mounts and removes the sheet in place, and the render-path restore stays as the net for theme/language, with `scroll-behavior` suppressed during the write and re-asserted on the next frame. Locking the page behind it (`:has(> .landing-menu){overflow:hidden;scrollbar-gutter:stable}`) needed the gutter kept: hiding `overflow` removes the scrollbar, the text reflows wider, the page shortens, and the offset clamps again - the same 28px, found at the two widths straddling a breakpoint. Two lessons recorded the hard way: **a source-shape guard is not a behaviour guard** (my `withLandingScroll` assertion passed while the browser measured a jump), and my `restoreLandingScroll` threw `authored is not defined` in the browser while **721 unit assertions stayed green**, because jsdom has no layout so `if(!y) return` skipped the body - the new unit test *calls* the function so the body executes. Then the screenshot caught what no suite did: `--landing-bar-h` derived from `padding + tap` was a guess, the wordmark wraps to two lines at 390px, the bar is ~78px and "Ride" was hidden under it - so the bar's height is measured where the sheet is mounted (the same measure-and-publish idiom as the masthead's `--vh`), and the suite now asserts no row starts above the bar's bottom edge at any id. unit 724/0, landing 3221/0 (from 2773 at HEAD), a11y 14/0, layout 7570/0. | FIXED |
| G-112 | A gate gap and a stale fixture, found by the download feature itself (FIXED, round 11) | Two days after the installer landed in the repo, `apps/web/tests/server.test.js` was 6/2: the tests that assert *"with nothing staged the download redirects to the release"* got a 200 instead, because round 10 added a third source the server will serve - `apps/web/downloads/android.apk` - and the fixture only hid `ANDROID_APK_PATH`. My round-10 run of that file was genuinely green at the time; **the commit that made the feature work is the commit that invalidated its own test**, one step later, which is the shape of bug a fixture with a hidden assumption produces. Two real problems, fixed in that order: the phrase "nothing staged" now means all three candidates (a `withNoInstaller()` helper moves each file aside and puts it back, so the test reads the same in a checkout with or without a committed installer), and `apps/web/verify.sh` — the script CI actually runs — **never invoked `tests/server.test.js` at all** even though `package.json`'s `test` script does, so no push could have surfaced it: the route tests are now in the gate. Lesson kept: when a change adds a *source* to a lookup chain, audit every test that hides one, and check which entry point the runner uses rather than assuming `test` is what CI calls. | FIXED |
